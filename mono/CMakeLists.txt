# zlib first: it may define the ZLIB::ZLIB everything else links against.
add_subdirectory(zlib)
add_subdirectory(eglib)
add_subdirectory(utils)
add_subdirectory(culture)
add_subdirectory(metadata)
if(MONO_ENABLE_SGEN)
  add_subdirectory(sgen)
endif()
add_subdirectory(mini)
if(MONO_ENABLE_INTERPRETER)
  # After mini/: mini owns the targets this hangs off.
  add_subdirectory(interp)
endif()
add_subdirectory(cil)
add_subdirectory(dis)
if(MONO_ENABLE_PROFILER)
  add_subdirectory(profiler)
endif()
if(MONO_ENABLE_MONO_NATIVE)
  add_subdirectory(native)
endif()
if(MONO_ENABLE_LLVM)
  add_subdirectory(llvm)
endif()
if(BUILD_TESTING)
  add_subdirectory(unit-tests)
  add_subdirectory(tests)
  add_subdirectory(benchmark)
endif()
