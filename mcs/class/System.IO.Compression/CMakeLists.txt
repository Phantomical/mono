# Converted from class/System.IO.Compression/Makefile.

mono_declare_managed(
  NAME       System.IO.Compression.dll
  PROFILES   build net_4_x unityjit
  KEYFILE    ../ecma.pub
  REFS       System
  FLAGS      -unsafe
)
